version: '3'
services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
  producer:
    build:
      context: .
    depends_on:
      - rabbitmq
    command: ["/bin/bash", "-c", " sleep 15 && . venv/bin/activate && python producer.py"]
  consumer:
    build:
      context: .
    depends_on:
      - rabbitmq
    command: ["/bin/bash", "-c", " sleep 10 && . venv/bin/activate && python consumer.py"]
